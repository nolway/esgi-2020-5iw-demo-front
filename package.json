{
    "private": true,
    "dependencies": {
        "@vue/cli-service": "^4.5.4",
        "director": "^1.2.0",
        "todomvc-app-css": "^2.0.0",
        "todomvc-common": "^1.0.1",
        "vue": "^2.1.8"
    },
    "scripts": {
        "build": "npx vue-cli-service build js/app.js",
        "predeploy": "npm run build",
        "deploy": "gh-pages -d build"
    },
    "devDependencies": {
        "gh-pages": "^3.1.0"
    }
}